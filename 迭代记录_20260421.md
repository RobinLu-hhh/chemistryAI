# ChemAI 系统迭代记录

## 日期：2026-04-21

---

## 一、核心产品定位

**ChemAI** = 化学教育AI系统 + **持久进化的Agent学习系统**

### 关键设计原则
1. **Human-in-the-Loop** - 老师确认是核心环节，不能跳过
2. **持久进化** - 每次诊断结果写入学生memory.md，下次更精准
3. **统一解析体系** - OCR + MinerU + 多模态 是一个完整体系，不能拆分
4. **智能功能走Agent** - 只有智能功能（OCR识别、诊断分析、出题等）走ChemAI Agent

---

## 二、支持的文档格式（完整列表）

### 文档解析服务 `DocumentParseService`
| 格式 | 后缀 | 解析方式 |
|------|------|----------|
| PDF | .pdf | MinerU |
| Word | .docx | MinerU |
| PowerPoint | .pptx | MinerU |
| 图片 | .png/.jpg/.gif | OCR优先 → 视觉模型降级 |

**检测逻辑：**
- 文件头 `%PDF` → PDF
- JPEG/PNG/GIF文件头 → 图片
- ZIP格式 → 检测内部结构判断docx/pptx

---

## 三、完整学情流程（已贯通）

```
老师上传答题卡/试卷
    ↓
[OCR/MinerU/视觉模型] 自动选择最佳解析方式
    ↓
预览弹窗显示识别结果 + 【学生身份确认区域】（新增）
    ↓
老师确认/修正学生身份（学号+姓名）
    ↓
老师确认答案
    ↓
系统调用 /api/ocr/confirm 生成统计和诊断
    ↓
【诊断结果持久化】调用 /api/memory/diagnosis-complete（新增）
    ↓
诊断结果写入学生 memory.md（新增）
    ↓
学生画像持续进化
```

---

## 四、看面板功能（已完成）

### 班级整体视图
- 概览统计（学生数、考试次数、平均正确率、完成率）
- 成绩趋势图
- 障碍类型分布饼图
- **学生卡片网格（可点击）**
- **搜索框（姓名/学号）**
- **障碍类型筛选**
- **排序（姓名/正确率/障碍类型）**
- 班级对比

### 单一学生视图（点击学生卡片进入）
- 学生概览
- 障碍分布
- 薄弱知识点
- 学习历史
- 个性化学习计划（可生成）

---

## 五、已创建/修改的文件

### 新建文件
| 文件 | 说明 |
|------|------|
| `app/api/memory.py` | 学情记忆API（诊断持久化端点） |

### 修改文件
| 文件 | 修改内容 |
|------|----------|
| `frontend/teacher_v2.html` | 添加学生身份确认UI、面板容器 |
| `frontend/src/modules/teacher/ocr.js` | 学生身份状态、确认函数、持久化调用 |
| `frontend/src/modules/teacher/panel.js` | 班级/学生视图切换、搜索筛选排序、学生卡片 |
| `app/services/document_parse_service.py` | 添加docx/pptx检测和解析方法 |
| `app/main.py` | 注册memory路由 |

---

## 六、记忆系统（已实现）

### chemistry-memory Handler
- `memory_student_get()` - 获取学生记忆
- `memory_student_update()` - 更新学生记忆
- `memory_on_diagnosis_completed()` - 诊断完成时写入memory.md
- `memory_on_exam_completed()` - 考试完成时追加记录
- `memory_search()` - 跨记忆搜索

### 记忆类型
- `profile.md` - 学生画像摘要
- `barrier_history.md` - 障碍诊断历史
- `weak_kps.md` - 薄弱知识点追踪
- `practice_history.md` - 练习历史

---

## 七、ChemAI Agent 工具链

### chemistry-parser
- `smart_parse_document()` - 智能文档解析（自动选择OCR/MinerU/Vision）
- `import_question_bank()` - 上传真题到题库
- `parse_pdf_questions()` - PDF解析
- `get_services_status()` - 获取服务状态

### chemistry-diagnosis
- `diagnosis_barrier_class()` - 班级障碍诊断
- `diagnosis_barrier_student()` - 学生障碍详情
- `diagnosis_plan_generate()` - 生成学习计划
- `diagnosis_plan_apply()` - 应用学习计划

### chemistry-memory
- `memory_on_diagnosis_completed()` - 诊断持久化
- `memory_summarize()` - 生成画像摘要

---

## 八、API端点汇总

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/ocr/preview` | POST | OCR预览+LLM分析 |
| `/api/ocr/confirm` | POST | 确认OCR结果生成统计 |
| `/api/ocr/parse/document` | POST | 统一文档解析入口 |
| `/api/memory/diagnosis-complete` | POST | 诊断结果持久化 |
| `/api/memory/student/{id}` | GET | 获取学生记忆 |
| `/api/memory/stats` | GET | 记忆系统统计 |
| `/api/diagnosis/barrier/{class_id}/{exam_id}` | GET | 班级障碍诊断 |
| `/api/diagnosis/learning-plan/generate` | POST | 生成学习计划 |

---

## 九、待完善/注意事项

1. **classId问题** - 目前OCR确认时使用硬编码的`demo_class`，需要改为从当前班级选择
2. **学生记忆读取** - 诊断时应先读取学生memory.md结合历史，不只是当前考试数据
3. **MinerU路径** - 需确认 `D:\化学\MinerU-master\MinerU-master` 可用
4. **ZHIPU_API_KEY** - OCR和视觉模型需要配置

---

## 十、前端技术栈

- `teacher_v2.html` - 教师端主页面
- `src/modules/teacher/` - 各功能模块
  - `ocr.js` - OCR识别模块
  - `exam.js` - 考试管理模块
  - `panel.js` - 学情面板模块
  - `diagnosis.js` - 诊断模块
  - `report.js` - 报告模块
- `src/services/hermes.js` - ChemAI Agent服务
- `src/services/exam.js` - 考试服务API

---

## 十一、沟通原则（来自用户反馈）

1. **文档解析体系** - 必须说"OCR/MinerU/多模态"，不能只说"OCR"
2. **支持格式** - 必须列出完整列表，不能只列"PDF和图片"
3. **UI效率** - 避免低下拉框选择，使用搜索+筛选+卡片点击
4. **沟通语言** - 必须使用简体中文
