# ChemAI 项目修复计划

> 基于 2026-04-24 全面代码审查，按优先级排序

---

## 目录

1. [P0 紧急修复（功能不可用）](#p0-紧急修复功能不可用)
2. [P1 重要修复（功能缺失）](#p1-重要修复功能缺失)
3. [P2 中等修复（功能受限）](#p2-中等修复功能受限)
4. [P3 优化改进（体验/代码质量）](#p3-优化改进体验代码质量)
5. [修复执行顺序建议](#修复执行顺序建议)

---

## P0 紧急修复（功能不可用）

### P0-1：ChemAI Agent 请求路径不匹配

**现象**：前端点击题目/功能 → 提示"Agent未启动"

**根因**：
- 前端 `frontend/src/services/hermes.js:181` 调用 `${HERMES_API}/chat/completions`
- 后端路由 `app/api/hermes_proxy.py:37` 定义为 `@router.post("/v1/chat/completions")`
- `HERMES_API = http://localhost:8001/api/hermes`
- 前端实际请求：`/api/hermes/chat/completions`（缺少 `/v1/` 前缀）
- 后端期望路径：`/api/hermes/v1/chat/completions` → 404 不匹配

**修复方案**：
```
文件：frontend/src/services/hermes.js
修改：HERMES_API = ${API_BASE_URL}/api/hermes/v1
      → 使得 ${HERMES_API}/chat/completions = /api/hermes/v1/chat/completions
```

**影响范围**：`hermes.js` 第 16-18 行和所有使用 `HERMES_API` 的地方（`execute`、`executeStream`、`listenToEvents`、`confirm`、`correct`、`interrupt` 方法）

**工作量**：小（修改 1 个文件，改 1 行常量定义）

---

### P0-2：历年真题数据在数据库查询中永远为空

**现象**：前端"历年真题"页面 → 显示"无数据"

**根因**：
- `ExamBankService`（`app/services/exam_bank.py`）从 `data/exam_questions/*.json` 加载 250 道真题到**内存**，工作正常
- `GET /api/exam-bank/historical`（`app/api/exam_bank.py:390`）查询的是 `HistoricalExam` **数据库表**
- 数据库中 `historical_exams` 表从未被任何初始化/导入脚本填充过

**修复方案**（二选一）：

**方案 A（推荐）**：将 exam_bank API 改为从 `ExamBankService` 内存服务读取
```
文件：app/api/exam_bank.py
修改：get_historical_questions() 函数
改造：不再查询 db.query(HistoricalExam)，改为调用 exam_bank_service.search_questions()
保留：分页、筛选参数逻辑不变
```

**方案 B**：创建导入脚本 `import_exam_data.py` 将 JSON 数据导入 `historical_exams` 表
- 需要维护两份数据同步，不推荐

**影响范围**：`app/api/exam_bank.py` 的 `GET /historical` 端点

**工作量**：中（改 1 个 API 端点，重写查询逻辑）

---

### P0-3：前端角色常量与后端角色值不匹配

**现象**：登录后（如用 hai/[REDACTED] 学科组长），前端菜单显示不全，功能受限

**根因**：
- 后端 `init_db.py` 创建的角色值：`"admin"`, `"学科组长"`, `"teacher"`, `"教务管理员"`
- 前端 `auth.js:14-18` 的 `UserRole`：
  ```js
  ADMIN: 'admin',
  TEACHER: 'teacher',
  STUDENT: 'student'
  ```
- `isTeacher()` 返回 `user.role === 'teacher'`，对 `role="学科组长"` 返回 `false`
- 前端各页面据此控制菜单显隐，导致学科组长看不到应有的功能

**修复方案**：
```
文件：frontend/src/services/auth.js
修改：
  1. 在 UserRole 中补充：
     ACADEMIC_LEADER: '学科组长',
     EDUCATION_ADMIN: '教务管理员'
  2. isTeacher() 方法改为：
     return ['teacher', '学科组长', '教务管理员'].includes(user.role)
  3. 新增 isAcademicLeader() / isEducationAdmin() 方法
```

**影响范围**：`auth.js` + 所有前端页面中引用 `isTeacher()` 进行权限判断的地方

**工作量**：小，但需要全局搜索 all `isTeacher()`/`isAdmin()`/`isStudent()` 调用点进行适配

---

### P0-4：前端页面未做登录校验

**现象**：直接访问 `teacher_v2.html` 或 `student_v2.html` 页面空白或报错

**根因**：
- `teacher_v2.html` 和 `student_v2.html` 页面加载时未检查 `sessionStorage` 中是否有用户登录信息
- 未登录直接访问时，页面尝试渲染需要用户数据的界面，抛出异常导致空白

**修复方案**：
```
在每个 HTML 页面的 <head> 内或首个 <script> 中添加：
<script>
  (function() {
    const user = sessionStorage.getItem('chemai_user');
    if (!user) {
      window.location.href = '/login.html';
      return;
    }
  })();
</script>
```

**影响范围**：`frontend/teacher_v2.html` + `frontend/student_v2.html` + `frontend/parent_portal.html`

**工作量**：小（每个文件加 6-8 行脚本）

---

## P1 重要修复（功能缺失）

### P1-1：start.bat 每次重启删除数据库

**现象**：每次重启服务后，所有配置和数据丢失

**根因**：`start.bat:13` 执行 `del chemai.db 2>nul` 无条件删除数据库

**修复方案**：
```
文件：start.bat
修改：
  - 删除 del chemai.db 这一行
  - 或改为交互式询问是否重置数据库：
    set /p RESET="是否重置数据库？(y/N): "
    if /i "%RESET%"=="y" del chemai.db 2>nul
```

**影响范围**：`start.bat`

**工作量**：极小

---

### P1-2：init_db 未创建任何演示考试/答题数据

**现象**：教师端面板、错题报告、诊断、学情分析均显示"无数据"

**根因**：`app/utils/init_db.py` 只创建了基础数据（学校/年级/教师/班级/学生/账号），未创建：
- `ExamRecord`（考试记录）
- `Question`（题目）
- `StudentAnswer`（学生作答）
- `BarrierConfig`（障碍诊断配置）

**修复方案**：
```
在 app/utils/init_db.py 中新增函数 create_demo_exam_data(db)：
  1. 创建一个演示考试 ExamRecord（如"示例班级A 第一次月考"）
  2. 创建 10-15 道模拟题目 Question（覆盖盐类水解、氧化还原、原电池等知识点）
  3. 为 67 名学生创建随机 StudentAnswer 记录（包含正确/错误答案）
  4. 为教师创建 BarrierConfig
  5. 更新班级/考试的统计字段

在 init_database() 末尾调用 create_demo_exam_data(db)
```

**影响范围**：`app/utils/init_db.py`

**工作量**：大（约 150-200 行代码），但可复用已有的数据模型

---

### P1-3：login.html 演示账号硬编码且与数据库不匹配

**现象**：
- 快速登录"管理员"存的是 `{school: "演示学校"}`，但数据库实际是"演示学校"
- 快速登录"任课教师"存的是"教师D"，但数据库是"教师A"/"教师B"

**根因**：`frontend/login.html:962-983` 的 `DEMO_ACCOUNTS` 是硬编码 mock 数据

**修复方案**：
```
方案 A（推荐）：快速登录改为调用真实后端 API
  quickLoginAs(role) {
    const credentials = {
      admin: { username: 'admin', password: 'admin123' },
      academic: { username: 'hai', password: '[REDACTED]' },
      teacher: { username: 'liu', password: '[REDACTED]' },
      student: { username: 'student_demo_001', password: '[REDACTED]' },
      parent: { username: '...', password: '...' }  // 家长待创建
    };
    // 调用真实的 /api/auth/login 接口
    const result = await apiRequest('/api/auth/login', {
      method: 'POST',
      body: JSON.stringify(credentials[role])
    });
    // 用后端返回的真实用户信息存 session
  }

方案 B：同步修改 DEMO_ACCOUNTS 为数据库实际数据
  但维护两份数据容易再次脱节，不推荐
```

**影响范围**：`frontend/login.html` 的 JavaScript 部分

**工作量**：中（重写 1 个函数）

---

### P1-4：login.html Fallback 用户集与数据库不匹配

**现象**：后端 API 登录失败后 fallback 到 mock 用户，用户用真实账号登录提示"密码错误"

**根因**：`frontend/login.html:1119-1127` 的 `handleLoginFallback` 使用另一套 mock 用户表

**修复方案**：
```
文件：frontend/login.html
修改 handleLoginFallback()：
  方案：统一为与数据库匹配的用户名/密码
  const mockUsers = {
    'admin': { ... , password: 'admin123' },
    'hai': { name: '教师A', role: '学科组长', ... , password: '[REDACTED]' },
    'liu': { name: '教师B', role: 'teacher', ... , password: '[REDACTED]' },
    'chen': { name: '教师C', role: 'teacher', ... , password: '[REDACTED]' },
    'student_demo_001': { name: '学生A', role: 'student', ... , password: '[REDACTED]' },
  };
  - 删除不存在的用户（academic、teacher、student、13800000001）
  - 补充数据库实际存在的用户
```

**影响范围**：`frontend/login.html`

**工作量**：小

---

## P2 中等修复（功能受限）

### P2-1：教师端/学生端 SPA 页面可能调用不存在的 API

**现象**：前端某些功能点击后报错或无响应

**根因**：需要全面审计 `teacher_v2.html` 和 `student_v2.html` 中所有 API 调用路径是否与后端匹配

**修复方案**：
```
审计步骤：
  1. 查看 teacher_v2.html 中引用的 JS 文件（是 app.js 还是 dist/ 下的构建文件？）
  2. 如果是 SPA 方式，找出所有 fetch/api 调用
  3. 逐个对比后端 app/api/ 下的路由定义
  4. 修复不匹配的路径
```

**工作量**：中（需要审计具体调用了哪些端点）

---

### P2-2：教务管理员/学科组长前端权限菜单未实现

**现象**：`auth.js` 无法识别"教务管理员"和"学科组长"角色

**根因**：`ROLE_PERMISSIONS` 在后端已定义了完整的五级权限体系，但前端没有对应的角色识别和菜单控制逻辑

**修复方案**：
```
1. 在 auth.js 中补充角色识别方法：
   - isTeacher() 扩展为 isTeacherLike() 包含 teacher/学科组长/教务管理员
   - 新增 getRoleLevel() 返回角色等级数字，方便比较
2. 在 teacher_v2.html 的菜单渲染逻辑中，根据 role 显示/隐藏菜单项
```

**工作量**：中

---


### P2-4：teacher.html 与 teacher_v2.html 新旧版本共存

**现象**：可能存在两份教师端页面，代码不同步

**根因**：项目有 Vite 构建流程，`dist/` 下是构建产物，但根目录也有 `teacher_v2.html`

**修复方案**：
```
1. 明确当前使用哪套前端
2. 如果使用 teacher_v2.html（非构建版本），清理 dist/ 下的冗余文件
3. 如果使用 vite 构建版本，统一构建流程
```

**工作量**：小

---

## P3 优化改进（体验/代码质量）

### P3-1：Google Fonts 外网依赖

**文件**：`teacher_v2.html:9`、`student_v2.html:9`

**建议**：替换为国内 CDN 或本地字体文件

---

### P3-2：JWT 密钥硬编码

**文件**：`app/middleware/auth.py:16`

**建议**：改为从环境变量读取，无设置时自动生成随机密钥并提示

---

### P3-3：CORS 配置过于宽松

**文件**：`app/main.py:39`

**建议**：生产环境改为白名单域名

---

### P3-4：start.bat 端口固定 8001

**文件**：`start.bat:18`

**建议**：改为从环境变量读取，或检测端口占用后自动递增

---

## 修复执行顺序建议

### 第一阶段：P0 紧急修复（让系统跑起来）

| 步骤 | 问题 | 预计耗时 | 前置依赖 |
|------|------|---------|---------|
| 1 | P0-1 Hermes 路径不匹配 | 5 分钟 | 无 |
| 2 | P0-2 真题数据显示 | 30 分钟 | 无 |
| 3 | P0-3 角色常量匹配 | 20 分钟 | 无 |
| 4 | P0-4 页面登录校验 | 10 分钟 | 无 |

第一阶段完成后可验证：Agent 可连接、真题可显示、各角色登录后功能完整

### 第二阶段：P1 重要修复（让功能完备）

| 步骤 | 问题 | 预计耗时 | 前置依赖 |
|------|------|---------|---------|
| 5 | P1-1 start.bat 不删库 | 5 分钟 | 无 |
| 6 | P1-2 创建演示考试数据 | 1-2 小时 | 无 |
| 7 | P1-3 快速登录对接真实 API | 20 分钟 | 后端 API 可用 |
| 8 | P1-4 Fallback 用户同步 | 10 分钟 | 无 |

第二阶段完成后可验证：重启不丢数据、各页面有演示数据展示

### 第三阶段：P2 中等修复（让体验流畅）

| 步骤 | 问题 | 预计耗时 |
|------|------|---------|
| 9 | P2-1 审计前端 API 调用 | 1 小时 |
| 10 | P2-2 完善前端权限菜单 | 30 分钟 |
| 11 | P2-4 清理前端版本混乱 | 30 分钟 |

### 第四阶段：P3 优化改进 ✅ 已完成

| 步骤 | 问题 | 状态 |
|------|------|------|
| 13 | Google Fonts 替换 | ✅ 已更换为 fonts.googleapis.cn |
| 14 | JWT 密钥环境变量化 | ✅ 已通过 CHEMAI_JWT_SECRET 实现 |
| 15 | CORS 白名单配置 | ✅ 已通过 CHEMAI_CORS_ORIGINS 实现 |
| 16 | 动态端口检测 | ✅ 已通过 CHEMAI_PORT 环境变量实现 |

---

## 快速启动验证清单

修复后验证以下场景是否正常：

- [ ] 启动服务 → 访问 `http://localhost:8001/` → 自动跳转登录页
- [ ] 输入 `hai / [REDACTED]` → 登录成功 → 跳转教师端
- [ ] 教师端左侧菜单：OCR、出题、报告、诊断、练习、学情面板、真题库 全部可见
- [ ] 点击"历年真题"或任意题目 → 不提示"Agent未启动"
- [ ] 真题库列表显示 250 道题目数据
- [ ] 快速登录"系统管理员" → 进入管理端 → 显示完整功能
- [ ] 快速登录"学生" → 进入学生端 → 有演示学习数据
- [ ] 重启服务 → 数据不丢失
- [ ] 直接访问 `teacher_v2.html` → 未登录时跳转登录页

---

## P5 后端未实现端点（待补齐功能）

> 以下端点在前端 `src/services/` 中已有调用代码，但后端 `app/api/` 中无对应路由实现。点击相关功能时会 404。

### P5-1：诊断模块（5 个端点）

| 前端方法 | 期望路径 | 文件 |
|---------|---------|------|
| `getInterventionPlan()` | `GET /api/diagnosis/plan/{student_id}` | `diagnosis.js:52` |
| `submitFeedback()` | `POST /api/diagnosis/{diagnosis_id}/feedback` | `diagnosis.js:59` |
| `getDiagnosisHistory()` | `GET /api/diagnosis/history/{student_id}` | `diagnosis.js:66` |
| `getClassBarrierStats()` | `GET /api/diagnosis/class/{class_id}/stats` | `diagnosis.js:73` |
| `getKPBarnerAnalysis()` | `GET /api/diagnosis/class/{class_id}/kp/{kp}` | `diagnosis.js:80` |

### P5-2：考试管理模块（3 个端点）

| 前端方法 | 期望路径 | 文件 |
|---------|---------|------|
| `updateExam()` | `PUT /api/exam/{exam_id}` | `exam.js:31` |
| `deleteExam()` | `DELETE /api/exam/{exam_id}` | `exam.js:38` |
| `getStudentResult()` | `GET /api/exam/{exam_id}/result/{student_id}` | `exam.js:52` |

### P5-3：练习模块（6 个端点）

| 前端方法 | 期望路径 | 文件 |
|---------|---------|------|
| `getPracticeQuestions()` | `GET /api/practice/{practice_id}/questions` | `practice.js:31` |
| `getPracticeHistory()` | `GET /api/practice/history/{student_id}` | `practice.js:62` |
| `getWrongQuestions()` | `GET /api/practice/wrong/list` | `practice.js:69` |
| `markAsMastered()` | `POST /api/practice/wrong/{question_id}/master` | `practice.js:76` |
| `getReviewQuestions()` | `GET /api/practice/review/list` | `practice.js:83` |
| `getHistoricalQuestions()` | `GET /api/practice/historical` | `practice.js:90` |

### P5-4：报告模块（6 个端点）

| 前端方法 | 期望路径 | 文件 |
|---------|---------|------|
| `getStudentReport()` | `GET /api/report/student/{student_id}` | `report.js:11` |
| `getClassReport()` | `GET /api/report/class/{class_id}` | `report.js:18` |
| `getTeacherReport()` | `GET /api/report/teacher/{teacher_id}` | `report.js:25` |
| `getKPMastery()` | `GET /api/report/student/{student_id}/kp-mastery` | `report.js:32` |
| `getBarrierChange()` | `GET /api/report/student/{student_id}/barrier-change` | `report.js:39` |
| `getScoreTrend()` | `GET /api/report/student/{student_id}/trend` | `report.js:46` |

注：后端报告模块路径格式为 `/api/report/{type}/{exam_record_id}/{student_id}`，与前端结构不同，可能需要统一设计。

### P5-5：题目管理模块（8 个端点）

| 前端方法 | 期望路径 | 文件 |
|---------|---------|------|
| `getQuestionDetail()` | `GET /api/question/{question_id}` | `question.js:48` |
| `approveQuestion()` | `POST /api/question/{question_id}/approve` | `question.js:55` |
| `rejectQuestion()` | `POST /api/question/{question_id}/reject` | `question.js:62` |
| `getCategories()` | `GET /api/question/categories` | `question.js:69` |
| `getKPs()` | `GET /api/question/kps` | `question.js:76` |
| `searchQuestions()` | `POST /api/question/search` | `question.js:41` |
| `getSimilarQuestions()` | `GET /api/question/similar/{question_id}` | `question.js:34` |

注：后端 `question.py` 有 `/similar`（POST，无路径参数），前端是 GET + 路径参数，两者格式不同。

### P5-6：家长端家长账号未创建

- 数据库 `accounts` 表中无家长角色账号
- 登录页"家长快速体验"按钮调用 `/api/auth/login` 会返回失败
- 需要新建家长账号并绑定学生

### 修复建议

```
各模块实现时参考以下步骤：
1. 在对应 app/api/xxx.py 中添加路由（参考已有路由风格）
2. 实现业务逻辑（可先用 mock 数据）
3. 确保响应格式与前端期望一致
4. 添加输入验证和错误处理

优先级建议：
  第一梯队（高频功能）：P5-1 诊断 + P5-5 题目管理
  第二梯队（考试流程）：P5-2 考试管理
  第三梯队（数据展示）：P5-4 报告模块
  第四梯队（练习模块）：P5-3 练习
```

---

*文档版本：v2.0 | 生成日期：2026-04-25 | 新增 P5 后端未实现端点清单*
