# ChemAI 系统开发计划

**创建日期**: 2026-04-22
**状态**: 待实施
**预计周期**: 7天

---

## 一、代码现状总结

### 1.1 已有的后端代码（可复用）

| 模块 | 文件 | 已有内容 |
|------|------|----------|
| 数据库模型 | `app/models/database.py` | Teacher, Student, Account, Class, Question, ExamRecord, BarrierConfig, KnowledgePoint |
| 认证API | `app/api/auth.py` | login, register, logout, me |
| 用户API | `app/api/user.py` | 学生/教师 CRUD, 审批 |
| 班级API | `app/api/class_api.py` | 班级 CRUD, 获取班级学生 |
| 其他业务API | ocr, exam, diagnosis, report, practice, panel, memory | 完整业务逻辑 |
| 数据库初始化 | `app/utils/init_db.py` | 创建默认admin和演示班级 |

### 1.2 已有的前端代码（可复用）

| 文件 | 已有内容 |
|------|----------|
| `teacher.html` | 侧边栏导航、页面切换、admin-only菜单隐藏 |
| `login.html` | 4级角色体验登录 |
| `student.html` | 学生端界面 |

### 1.3 已完成的功能

| 功能 | 状态 |
|------|------|
| 统一文档解析（PDF/DOCX/PPTX/图片） | ✅ 已完成 |
| 学生身份确认 + 诊断持久化 | ✅ 已完成 |
| 班级/学生视图切换 + 搜索筛选 | ✅ 已完成 |
| 登录页面（4级角色体验） | ✅ 已完成 |
| 权限与班级管理设计文档 | ✅ 已完成 |

---

## 二、需要补充/升级的部分

### 2.1 数据库层

**现有问题**：
- Class表只有单个teacher_id，但一个班应有多个任课教师
- 没有独立的年级表，grade只是Class表的字符串字段
- 没有学校表，school_id分散在各表
- Account.role 只有 admin/teacher/student 三种

**新增表**：

```sql
-- 1. 学校表
CREATE TABLE schools (
    school_id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    region VARCHAR(200),
    address VARCHAR(255),
    phone VARCHAR(20),
    current_term VARCHAR(50),
    subjects JSON,  -- ["语文","数学","英语","化学",...]
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 2. 年级表
CREATE TABLE grades (
    grade_id VARCHAR(64) PRIMARY KEY,
    school_id VARCHAR(64),
    name VARCHAR(50) NOT NULL,  -- "高一"
    year INT NOT NULL,  -- 2025
    status TINYINT DEFAULT 1,  -- 1:在读 0:已毕业
    created_at TIMESTAMP,
    FOREIGN KEY (school_id) REFERENCES schools(school_id)
);

-- 3. 教师-班级-科目关联表（多对多）
CREATE TABLE teacher_class_subject (
    id VARCHAR(64) PRIMARY KEY,
    teacher_id VARCHAR(64),
    class_id VARCHAR(64),
    subject VARCHAR(50),
    is_class_teacher BOOLEAN DEFAULT FALSE,  -- 是否班主任
    assigned_at TIMESTAMP,
    FOREIGN KEY (teacher_id) REFERENCES teachers(teacher_id),
    FOREIGN KEY (class_id) REFERENCES classes(class_id),
    UNIQUE KEY unique_teacher_class_subject (teacher_id, class_id, subject)
);

-- 4. 操作日志表
CREATE TABLE operation_logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id VARCHAR(64),
    action VARCHAR(50) NOT NULL,  -- create/update/delete/login
    target_type VARCHAR(50),  -- student/teacher/class/grade
    target_id VARCHAR(64),
    detail JSON,
    ip_address VARCHAR(50),
    created_at TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES accounts(account_id)
);

-- 5. 教师入驻申请表
CREATE TABLE teacher_applications (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(128) NOT NULL,
    phone VARCHAR(32) NOT NULL,
    school VARCHAR(200),
    subject VARCHAR(50),
    status VARCHAR(20) DEFAULT 'pending',  -- pending/approved/rejected
    reviewer_id VARCHAR(64),
    reviewed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**修改现有表**：

```sql
-- Class表添加grade_id外键
ALTER TABLE classes ADD COLUMN grade_id VARCHAR(64);
ALTER TABLE classes ADD FOREIGN KEY (grade_id) REFERENCES grades(grade_id);

-- Class表删除单teacher_id（改用teacher_class_subject关联）
ALTER TABLE classes DROP COLUMN teacher_id;

-- Teacher表添加role字段
ALTER TABLE teachers ADD COLUMN role VARCHAR(32) DEFAULT 'teacher';
-- role: admin / 教务管理员 / 学科组长 / teacher
```

### 2.2 认证层升级

**需要安装**：
```bash
pip install pyjwt bcrypt
```

**主要改动**：
1. 密码加密：SHA256 → bcrypt
2. Token方式：UUID → JWT
3. 新增权限中间件
4. 新增教师入驻申请流程

### 2.3 前端升级

**主要改动**：
1. teacher.html 动态菜单（根据角色显示不同菜单）
2. OCR上传时班级选择（从任教班级中选择）

---

## 三、详细实施计划

### Phase 1: 认证升级（1天）

| 序号 | 任务 | 文件 | 说明 |
|------|------|------|------|
| 1.1 | 安装依赖 | - | pip install pyjwt bcrypt |
| 1.2 | 创建权限中间件 | `app/middleware/auth.py` | require_auth, require_permission, can, ROLE_PERMISSIONS |
| 1.3 | 升级auth.py | `app/api/auth.py` | JWT token, bcrypt密码, 教师入驻申请端点 |
| 1.4 | 更新初始化数据 | `app/utils/init_db.py` | 更新演示账号支持新角色 |

### Phase 2: 数据库扩展（1天）

| 序号 | 任务 | 文件 | 说明 |
|------|------|------|------|
| 2.1 | 修改database.py | `app/models/database.py` | 添加School, Grade, TeacherApplication, OperationLog模型 |
| 2.2 | 创建数据库迁移脚本 | `app/utils/migrate.py` | ALTER TABLE语句，添加新表 |
| 2.3 | 更新init_db.py | `app/utils/init_db.py` | 创建演示学校、年级、班级关联数据 |

### Phase 3: 管理API（2天）

| 序号 | 任务 | 文件 | 说明 |
|------|------|------|------|
| 3.1 | 学校设置API | `app/api/school.py` | CRUD，学校信息配置 |
| 3.2 | 年级管理API | `app/api/grade.py` | CRUD，年级管理 |
| 3.3 | 教师入驻API | `app/api/teacher_application.py` | 申请、审核 |
| 3.4 | 教师管理增强 | `app/api/user.py` 或新建 `app/api/teacher.py` | 关联任教班级 |
| 3.5 | 学生管理增强 | `app/api/student.py` | 批量导入、批量调班 |

### Phase 4: 前端集成（2天）

| 序号 | 任务 | 文件 | 说明 |
|------|------|------|------|
| 4.1 | 动态菜单 | `frontend/teacher.html` | 根据role显示不同菜单 |
| 4.2 | OCR班级选择 | `frontend/teacher.html` | 上传答题卡前选择任教班级 |
| 4.3 | 管理页面 | `frontend/admin/*.html` | 学校/年级/班级/教师/学生管理页面 |

### Phase 5: 测试与完善（1天）

| 序号 | 任务 | 说明 |
|------|------|------|
| 5.1 | 全流程测试 | 登录→角色菜单→OCR识别→诊断→权限隔离 |
| 5.2 | 操作日志验证 | 检查关键操作是否记录 |
| 5.3 | 文档更新 | 更新API文档和使用说明 |

---

## 四、关键文件清单

| 操作 | 文件路径 | Phase |
|------|----------|-------|
| 新建 | `app/middleware/auth.py` | 1 |
| 修改 | `app/api/auth.py` | 1 |
| 新建 | `app/api/school.py` | 3 |
| 新建 | `app/api/grade.py` | 3 |
| 新建 | `app/api/teacher_application.py` | 3 |
| 修改 | `app/models/database.py` | 2 |
| 新建 | `app/utils/migrate.py` | 2 |
| 修改 | `app/utils/init_db.py` | 2 |
| 修改 | `frontend/teacher.html` | 4 |
| 新建 | `frontend/admin/*.html` | 4 |

---

## 五、演示学校演示数据

```python
# 学校
school = {
    "school_id": "minhua_001",
    "name": "演示学校",
    "region": "广东省深圳市",
    "subjects": ["语文", "数学", "英语", "化学", "物理", "生物"]
}

# 年级
grades = [
    {"grade_id": "grade_10", "name": "高一", "year": 2025},
    {"grade_id": "grade_11", "name": "高二", "year": 2024},
    {"grade_id": "grade_12", "name": "高三", "year": 2023},
]

# 班级
classes = [
    {"class_id": "class_10_1", "name": "示例班级A", "grade_id": "grade_10"},
    {"class_id": "class_10_2", "name": "示例班级B", "grade_id": "grade_10"},
    {"class_id": "class_11_1", "name": "示例班级C", "grade_id": "grade_11"},
]

# 教师
teachers = [
    {"teacher_id": "admin_001", "name": "系统管理员", "role": "admin"},
    {"teacher_id": "teacher_wang", "name": "王组长", "role": "学科组长", "subject": "化学"},
    {"teacher_id": "teacher_zhang", "name": "教师D", "role": "teacher", "subject": "化学"},
    {"teacher_id": "teacher_li", "name": "李老师", "role": "teacher", "subject": "数学"},
]

# 教师-班级-科目关联
teacher_class_subjects = [
    {"teacher_id": "teacher_wang", "class_id": "class_10_1", "subject": "化学", "is_class_teacher": True},
    {"teacher_id": "teacher_wang", "class_id": "class_10_2", "subject": "化学", "is_class_teacher": False},
    {"teacher_id": "teacher_zhang", "class_id": "class_10_1", "subject": "化学", "is_class_teacher": False},
    {"teacher_id": "teacher_zhang", "class_id": "class_10_2", "subject": "化学", "is_class_teacher": True},
    {"teacher_id": "teacher_li", "class_id": "class_10_1", "subject": "数学", "is_class_teacher": False},
]

# 演示账号（登录用）
demo_accounts = [
    {"username": "admin", "password": "admin123", "role": "admin"},
    {"username": "wang", "password: [REDACTED]", "role": "学科组长"},
    {"username": "zhang", "password: [REDACTED]", "role": "teacher"},
]
```

---

## 六、依赖项清单

```bash
pip install pyjwt bcrypt
```

---

## 七、验证检查清单

### Phase 1 验证
- [ ] 安装pyjwt和bcrypt成功
- [ ] 登录返回JWT token
- [ ] 教师入驻申请端点可用
- [ ] 权限中间件能正确拦截无权限请求

### Phase 2 验证
- [ ] 数据库新表创建成功
- [ ] 演示学校、年级、班级数据导入成功
- [ ] 教师-班级关联数据正确

### Phase 3 验证
- [ ] 年级CRUD正常
- [ ] 班级CRUD正常（关联年级）
- [ ] 教师入驻申请-审核流程正常
- [ ] 教师任教班级分配正常

### Phase 4 验证
- [ ] admin账号登录看到完整菜单
- [ ] 学科组长登录只看到学科相关菜单
- [ ] 任课教师登录只看到任教班级菜单
- [ ] OCR上传时能选择任教班级

### Phase 5 验证
- [ ] 跨角色数据隔离正确
- [ ] 操作日志记录完整
- [ ] 班级调班功能正常

---

## 八、风险与注意事项

### 8.1 数据库兼容性
- 使用SQLite（现有），确保ALTER TABLE兼容
- 如果切换到MySQL/PostgreSQL，需调整迁移脚本

### 8.2 Token刷新机制
- JWT access token有效期1小时
- refresh token有效期7天
- 前端需要在token过期前刷新

### 8.3 密码迁移
- 现有密码用SHA256存储
- 新注册用户用bcrypt
- 首次登录后可引导用户更新密码

### 8.4 数据兼容
- 现有exam_record等表通过class_id关联Class
- Class表结构调整不影响exam_record等表的外键关系
